# useForm

useForm is a hook adapter that exposes the internal form logic to React application lifecycle.

This hook represents a form on your page and matches the internal createForm method interface.
